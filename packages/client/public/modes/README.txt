Drop your three mode-card background images here with these exact names:

  duel.png      -> 1v1 DUEL card     (the urban cage / street court image)
  classic.png   -> 3v3 CLASSIC card  (the stadium + crowd image)
  aibots.png    -> AI BOTS MODE card  (the neon "turbo futsal" image)

Notes:
- PNG or JPG both work, but keep the filename/extension as ".png" (or change the
  url() in src/styles/app.css to match).
- Recommended size ~600x800 (portrait-ish) so they fill the cards cleanly.
- Until the files are added, each card shows a themed colour gradient so the page
  still looks finished.
